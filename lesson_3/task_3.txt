# 3. Создать пользователя с правами суперпользователя.
sudo addgroup sudo superuser

# Сделать так, чтобы sudo не требовал пароль для выполнения команд.
sudoedit /etc/sudoers # с помощью команды sudoedit открываем файл на редактирование
%sudo ALL=(ALL) NOPASSWD:ALL # вводим данную запись